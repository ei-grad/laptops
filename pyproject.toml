[project]
name = "laptops"
version = "0.1.0"
description = "Linux laptop research with structured data extraction"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.13.4",
    "python-frontmatter>=1.3.0",
]

[tool.ruff]
src = ["scripts"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.14",
]
